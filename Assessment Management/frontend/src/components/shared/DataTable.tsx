import type { ReactNode } from 'react'
export function DataTable({headers,children}:{headers:string[];children:ReactNode}){return <div className="card overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-slate-100 bg-slate-50/70 text-xs uppercase tracking-wider text-slate-500"><tr>{headers.map(x=><th key={x} className="whitespace-nowrap px-5 py-4 font-semibold">{x}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{children}</tbody></table></div></div>}
export function Pagination({page=1,total=1,onChange}:{page?:number;total?:number;onChange?:(p:number)=>void}){return <div className="mt-4 flex items-center justify-between text-sm text-slate-500"><span>Page {page} of {total}</span><div className="flex gap-2"><button disabled={page===1} onClick={()=>onChange?.(page-1)} className="btn-secondary px-3 py-1.5 disabled:opacity-40">Previous</button><button disabled={page===total} onClick={()=>onChange?.(page+1)} className="btn-secondary px-3 py-1.5 disabled:opacity-40">Next</button></div></div>}
