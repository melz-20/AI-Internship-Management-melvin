import { appSettings,adminProfile } from '../mock/data/seed';import { mockRequest } from '../mock/mockApi';import type { AppSettings,AdminProfile } from '../../types/settings';let settings={...appSettings};let profile={...adminProfile};export const settingsApi={get:()=>mockRequest(settings),save:(x:AppSettings)=>{settings=x;return mockRequest(x)},profile:()=>mockRequest(profile),saveProfile:(x:AdminProfile)=>{profile=x;return mockRequest(x)}}
